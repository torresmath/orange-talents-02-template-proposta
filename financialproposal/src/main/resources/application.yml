spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/financial_proposals?useTimezone=true&serverTimezone=America/Sao_Paulo
    username: root
    password: ajudazupper
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    format-sql: true
    hibernate.ddl-auto: update

management.endpoints.web.exposure.include: health

feign.hystrix.enabled: true

api:
  account.url: localhost:8888
  analysis.url: localhost:9999